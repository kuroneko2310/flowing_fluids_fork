# 開発メモ（AGENTS.md）

このファイルは本リポジトリ全体に適用される開発ガイドです。  
作業前に必ず読み、判断に迷ったときはこの方針を優先してください。

> この作業では、Forge 限定・最小変更・既存コード理解優先を徹底してください。  
> 他ローダー対応、共通化、将来拡張、要件外リファクタリングは行わないでください。  
> git の巻き戻し・破棄系操作は、必ずユーザー確認後に行ってください。  
> 軽量化を理由に処理・分岐・補助機構を増やしすぎず、総合的に見て単純で整合的な実装を優先してください。

---

## 最重要方針

- **この作業は Forge 限定で行います。**
- Fabric / NeoForge / マルチローダー化 / 将来の共通化は、明示的に要求されない限り今回の作業対象に含めません。
- **最小変更を最優先します。** 要件に直接必要な箇所だけを触り、無関係な整理・抽象化・最適化・共通化は行わないでください。
- **保守性は「構造を増やすこと」ではなく「読んで追えること」で守ります。**
- **コードの整合性を強く重視してください。** 局所的に動いて見えても、責務・命名・状態管理・分岐方針・データの流れが全体と噛み合わない変更は避けてください。
- 迷ったときは、**変更量が少なく、既存コードに沿っていて、後から戻しやすい案** を選んでください。
- **ゲーム挙動の安定性が最優先** です。流体物理や既存挙動に関わる変更は、プレイ感や互換性を壊さない範囲で行ってください。
- **既存ワールドの後方互換を守る** ことを重視してください。
- **軽量化は「処理を増やすこと」ではなく「全体として無駄を減らすこと」で判断してください。** 一部だけ速く見えても、補助処理・分岐・同期・管理コストを増やして総合的に重くなる変更は避けてください。

---

## Forge 限定ルール

- 今回の実装・修正・調査対象は **Forge 関連コードのみ** です。
- `common/` に既存ロジックがある場合でも、**Forge 実装に直接必要なときだけ** 読み、必要最小限で触ってください。
- `fabric/`, `fabric-like/`, `neoforge/` は今回の作業対象外です。
- 他ローダー側に似た処理や都合があっても、それを理由に Forge 側の設計を広げないでください。
- **「他でも使えそうだから共通化する」は禁止** です。
- Forge 側で完結するほうが読みやすく安全なら、共通化より **Forge 側への局所実装** を優先してください。
- Forge API に素直に沿えるなら、余計なラッパーや抽象層を足さず、そのまま扱ってください。

---

## 作業開始前の確認

- 修正や実装に入る前に、**現在のコードの流れ・既存責務・呼び出し関係・データの流れ** を十分に読んでください。
- 名前や見た目、断片だけで判断せず、**実際の使われ方と周辺処理** を確認してから変更してください。
- 既存コードに意図がありそうな処理は、不要に見えてもすぐ消さず、**なぜ存在しているかを先に読む** ことを優先してください。
- 「新しく作るほうが早そう」に見えても、まずは **今あるコードへ小さく沿わせて直せるか** を検討してください。
- 不具合修正では、症状だけでなく、再発条件・境界条件・関連する処理範囲も意識してください。
- 既存実装の流儀が明確な場合は、その流儀に合わせてください。自分の好みで構造を入れ替えないでください。
- 変更対象の前後だけでなく、**その処理が返す値・受け取る側・副作用の伝播先** まで確認してください。
- 局所修正で済ませる場合でも、**全体の整合性が崩れないか** を先に見てください。

---

## 実装時の判断基準

- 迷ったら **変更量が少ない案** を選んでください。
- 迷ったら **既存実装に似ている案** を選んでください。
- 迷ったら **クラスや関数を増やさない案** を選んでください。
- 迷ったら **Forge API に素直な案** を選んでください。
- 迷ったら **あとから差分が読みやすい案** を選んでください。
- 「美しい抽象化」より、**追いやすさ・壊れにくさ・戻しやすさ** を優先してください。
- 「将来使うかもしれない」は実装理由になりません。**今必要な要件** にだけ応えてください。
- **軽量化を理由に新しい状態管理、監視処理、補助キャッシュ、追加同期、複雑な近道をむやみに導入しないでください。**
- 単発の処理コストだけでなく、**常時コスト・保守コスト・分岐増加・不整合リスク** を含めて判断してください。
- **コード全体の整合性が崩れる案は採用しないでください。** たとえ局所的に便利でも、命名・責務・所有関係・更新タイミング・状態遷移が既存の流れから浮く変更は避けてください。
1つのファイルや1つの処理に責務を詰め込みすぎないでください。
実装時は、処理をその場に追加する前に、本当にその場所に置く必要があるか を考えてください。
役割が分かれたほうが自然で追いやすい場合は、複数ファイルや複数メソッドに分ける ことを検討してください。
逆に、分割のための分割は行わず、不要なら増やさない ことを優先してください。
1つの処理に分岐・責務・例外対応を抱え込ませすぎず、その処理が何を担当する場所なのかが読んでわかる粒度 を保ってください。
保守性は、何でも1か所に集めることではなく、責務が自然に分かれ、全体の流れを追いやすいこと で守ってください。

---

## 禁止事項

以下は、明示的な要求がない限り行わないでください。

- 要件にないリファクタリング
- 要件にない命名変更
- 要件にないファイル移動・分割・統合
- 要件にない設定項目追加
- 要件にないログ追加
- 要件にない抽象クラス・インターフェース・ヘルパー導入
- 要件にないキャッシュ・最適化
- 要件にない将来拡張ポイントの追加
- 要件にない共通化・汎用化
- 「きれいだから」「一般化できそうだから」という理由だけでの構造変更
- 他ローダーを見越した設計変更
- 無関係な import 整理や整形だけを含む差分の増加
- 実害確認なしの大規模な書き換え
- **軽量化の名目で、常時走る補助処理・監視処理・キャッシュ更新処理・分岐を増やすこと**
- **軽量化の名目で、局所最適のために全体の見通しや整合性を壊すこと**
- **便利そうという理由だけで補助機能や保険処理を増やすこと**
- **一貫した責務分担を崩す場当たり的な処理追加**

---

## 変更の進め方

作業は次の順序を基本にしてください。

1. まず要求を短く整理し、**今回どこまで触るか** を狭く定義する  
2. 変更が必要なファイルだけを特定する  
3. 対象コードと周辺コードを読み、既存の流れを把握する  
4. まず **最小実装** を作る  
5. そのあとで不要な重さや重複がないかだけ見直す  
6. 要求にない改善は、必要なら提案に留め、勝手に実装しない  
7. 差分を見直し、無関係な変更が混ざっていないことを確認する  
8. **局所的に直っていても、全体の責務・命名・状態遷移・更新順序が揃っているかを確認する**  
9. **軽量化や最適化を入れた場合は、増えた処理と減った処理を比較し、総合的に本当に得かを見直す**

---

## 軽量化・最適化のルール

- **軽量化を理由に処理を増やしすぎないでください。**
- 一部の計算回数を減らしても、そのために追加の状態管理・前計算・キャッシュ維持・無効化処理・同期処理が必要になるなら、**総合的に重くなる可能性** を先に疑ってください。
- 最適化は、**ボトルネックが明確な場合だけ** 検討してください。
- 推測だけで複雑な最適化を入れず、まずは単純で追いやすい実装を優先してください。
- 常時走る処理、毎 tick 処理、広範囲に波及するフック、監視型の仕組みは特に慎重に扱ってください。
- 軽量化は、CPU時間だけでなく、メモリ、分岐の増加、同期コスト、デバッグ困難性、不整合リスクまで含めて判断してください。
- 局所ベンチの印象だけでなく、**普段のプレイ全体での負荷** を意識してください。
- 「軽量化したつもり」で終わらせず、**処理総量が増えていないか** を見直してください。
- 軽量化より先に、不要処理を増やさない・責務を濁らせない・分岐を増やしすぎないことを優先してください。

---

## コードの整合性ルール

- **コードの整合性を常に保ってください。**
- 局所的に動作しても、全体の責務分担、命名規則、状態管理、null の扱い、例外方針、更新タイミング、依存方向が崩れる変更は避けてください。
- 同じ概念に対して複数の表現や似た責務を増やさず、**既存の中心となる場所に寄せる** ことを優先してください。
- 一時対応を入れる場合でも、既存のデータフローや所有関係を壊さないようにしてください。
- 似た処理が複数箇所に散る場合は、すぐ抽象化するのではなく、まず **なぜ散っているのか** を確認してください。
- 新しいフラグ、状態、補助メソッドを足すときは、**誰が責任を持って更新し、どこで参照され、いつ無効になるのか** を明確にしてください。
- 条件分岐は増やしすぎず、条件の意味が追えるようにしてください。
- 局所修正によって別の経路だけ規約が変わる、という状態を作らないでください。
- **「とりあえずここでも補正する」は整合性を崩しやすいので避けてください。**
- 不具合回避のための例外ルートを追加する場合は、通常ルートとの関係を明確にしてください。

---

## コメントと説明

- コメントは、**コードを読んだだけでは意図が追いにくい箇所** に付けてください。
- コードを読めばわかる処理に説明コメントを付けすぎないでください。
- 分岐条件、互換性維持、負荷回避、回避策、Forge 固有事情、バグ回避理由は短く明記してください。
- コメントは処理の逐語説明ではなく、**なぜその形で守っているか** を中心に書いてください。
- 長い講義のようなコメントや、設計思想を語りすぎるコメントは避けてください。
- 新しい設定項目やコマンドを追加する場合は、用途・効き方・触り方があとから見てわかるようにしてください。
- プレイヤー向けメッセージと開発者向けログは分けて考えてください。

---

## コードスタイルと命名

- 言語は主に Java を前提とし、Kotlin を使う場合も既存スタイルを踏襲してください。
- パッケージ構成、クラス命名、メソッド分割は **今のコードベースの流儀** を優先してください。
- 定数・設定キー・フラグ名は、意味が追いやすい名前にしてください。
- パフォーマンスや互換性に関わる値や制限は、必要に応じて理由が追える形にしてください。
- 例外を飲み込まないでください。握りつぶす必要がある場合は、理由がわかるようにしてください。
- ログは必要最小限にし、挙動確認に本当に必要な場面だけで使ってください。
- **同じ役割のものに別々の命名規則を持ち込まないでください。**
- **似た責務を持つ処理は、既存コードの粒度と揃えてください。**

---

## テストと検証

- 可能な限り `./gradlew build` を実行して、全体のビルドとテストを確認してください。
- Forge 向けの動作確認が必要な場合は、Forge 側の run 構成や実行手順を使って検証してください。
- パフォーマンスや挙動に関わる変更では、再現条件・境界条件・サーバー/クライアント差分を意識してください。
- Chunk 境界、tick 単位の揺れ、描画差分、同期差分など、Minecraft 特有の崩れ方に注意してください。
- 変更後は「ビルドが通る」だけでなく、**既存挙動を壊していないか** も確認してください。
- Forge 向けビルドが成功した場合は、生成 jar をこのインスタンスの `D:\CurseForge\Instances\main (1)\mods\` に配置して、すぐゲーム内確認できる状態にしてください。
- 最適化や軽量化を入れた場合は、**追加した仕組みの維持コストに見合っているか** を見直してください。
- 整合性に関わる変更では、対象箇所だけでなく、**その値や状態を読む側の動作** も確認してください。

---

## mixin と build 設定

- **mixin 設定や build 設定（Gradle 含む）の変更は事前合意必須** です。
- 挙動・設定を変える意図がない場合、mixin 周りの記述は不用意に触れないでください。
- build 設定は影響範囲が広いため、変更時は理由・影響範囲・確認内容を整理してください。
- 「ついでの整理」で mixin や build を触らないでください。

---

## 文字コードと改行コード

- パスの取得、確認、ファイル操作は **必ず PowerShell 経由** で行ってください。
- 別シェルや別ツールによる保存・変換で文字化けや改行混在を起こさないようにしてください。
- PowerShell 上では UTF-8 / LF を前提に扱ってください。
- CSV は **BOM 付き UTF-8 / CRLF** を使ってください。
- Markdown / YAML / TOML / Text などのテキストファイルは **BOM なし UTF-8 / LF** を使ってください。
- PowerShell 5.x のソースは **BOM 付き UTF-8 / LF**、PowerShell 7.x のソースは **BOM なし UTF-8 / LF** を使ってください。
- 文字コードが怪しいファイルに遭遇した場合、勝手に再エンコードせず、まず現状の保存形式と影響範囲を確認してください。

---

## バグ修正メモ

- エラーやバグを修正したときは、その場しのぎで終わらせず、**原因・再発条件・今後避けるべき実装方針** を記録してください。
- 記録先は `docs/` 配下のメモや既存の調査メモを優先してください。
- 変更を戻した場合も、「何が危なかったか」「次に試すならどこまで安全か」を短く残してください。
- 同じ事故を繰り返さないための判断材料を残すことを重視してください。

---

## 変更の区切りと自己レビュー

- ある程度変更が溜まった時点、またはひとまとまりの実装が終わった時点で、一度立ち止まって自己レビューを行ってください。
- 自己レビューでは、次の点を確認してください。

### 自己レビュー項目
- この変更は本当に Forge 側だけで完結しているか
- 要件にない抽象化や共通化を足していないか
- 無関係なファイルまで触っていないか
- 既存ワールド互換や既存挙動を壊していないか
- 差分があとから読みやすいか
- 元に戻しやすい変更単位になっているか
- コメントや説明が不足していないか
- 命名が既存コードから浮いていないか
- 「直したつもり」で終わらず、再発条件まで見ているか
- **軽量化のために処理や管理コストを増やしすぎていないか**
- **局所的な都合で全体の整合性を壊していないか**
- **責務・状態・更新順序・依存方向に矛盾がないか**

- 気づいた改善点は、その場で修正し、必要なら追加の確認やビルドを通してから次へ進んでください。

---

## Git 運用ルール

- 作業中は、節目ごとに `git status` や差分を確認し、**作業ツリーの状態を常に把握** してください。
- 変更がひとまとまりになったら、自己レビュー後に **できるだけ早めにコミットして状態を区切る** ことを推奨します。
- ただし、雑な途中状態を反射的にコミットするのではなく、**戻しやすく、意味が追える単位** に整えてからコミットしてください。
- 無関係な差分を混ぜず、コミットごとに目的がわかる状態を目指してください。
- コミット前には、意図しない変更ファイルや整形差分が混ざっていないかを見直してください。
- 大きな修正、本流へ載せる前の検証、危ない実験、並行作業では `git worktree` を積極的に活用してください。
- 作業ブランチ、worktree、stash、未コミット差分の役割を混ぜないようにしてください。

---

## Git の破壊的操作に関する厳守ルール

- **巻き戻し系の操作は、必ずユーザーに確認を取ってから行ってください。**
- 確認なしに以下を実行しないでください。

### ユーザー確認が必須の操作
- `git reset --hard`
- `git checkout -- <path>`
- `git restore`
- `git revert`
- 履歴の付け替え
- 変更破棄
- 削除済み変更の復元
- 強制上書きに近い差分整理
- 「汚れているから捨てる」という判断での破棄

- 競合解消や差分整理のつもりでも、**既存変更を失う可能性がある操作は独断で実行しないでください。**
- 「壊れているから戻す」を AI が勝手に確定しないでください。
- 戻す提案をすることはできますが、**実行はユーザーの明示的な許可後** にしてください。

---

## アセットと翻訳

- テクスチャ・モデル・言語ファイルは、Forge 側の適切なパスに配置してください。
- 解像度、キー命名、名前空間は既存ルールに合わせてください。
- アセット追加時は、後から見て用途や所属がわかるようにしてください。
- 翻訳キーは一貫性を保ち、場当たり的な命名を避けてください。

---

## コミット・PR 運用

- WHAT / WHY / HOW を簡潔にまとめてください。
- 破壊的変更の有無、実行したコマンド、確認した内容が追えるようにしてください。
- ビジュアル変更を含む場合は、スクリーンショットや動画があるとレビューしやすくなります。
- 1コミット1目的を意識し、あとから履歴を読んで流れが追えるようにしてください。

---

## 応答と提案の方針

- 回答は基本的に日本語で、読み手がすぐ理解できるように平易に書いてください。
- 実装・修正タスクでは、発想を広げることよりも、**まず要求された変更を最短で正確に形にする** ことを優先してください。
- 別案や拡張案は、実装を終えてから必要なときだけ短く添えてください。
- 説明だけで終わらせず、可能な限り **実際の変更案・コード案・差分の意図** まで降ろしてください。
- ただし、要件を超える提案をそのまま勝手に実装しないでください。
- 可読性・扱いやすさ・あとから手入れしやすいことを大切にしてください。
- **余計な保険処理や便利機能を足して「親切にしたつもり」にならないでください。**
- **必要十分で止め、整合性の高い実装を優先してください。**

---

## このリポジトリで特に慎重に扱う対象

### Fluid State Core Rule
- Treat fluid state construction as core gameplay logic for this mod.
- Before changing `getNewLiquid`, `getOwnHeight`, `createLegacyBlock`, `setFluidStateAtPosToNewAmount`, or effective fluid state handling, verify state transitions, rendering, virtual waterlog interactions, and vanilla compatibility together.
- Prefer narrow overrides for virtual or special cases; do not broadly replace vanilla fluid state calculation unless the full downstream behavior has been reviewed.

- 上記に関わる箇所は、**ゲームプレイの核となる挙動** として扱ってください。
- 仮想 waterlog、描画、状態遷移、vanilla 互換、既存ワールド影響を切り離さず、まとめて見てください。
- 特殊ケース対応は **狭い override** を優先し、広く置き換える変更は十分に確認した場合に限ってください。

---

## 最後の確認

変更を完了したと判断する前に、必ず次を確認してください。

- 今回の作業は Forge 限定のまま収まっているか
- 現在のコードを十分読んだうえで触れているか
- 最小変更で済んでいるか
- 要件にない整理や抽象化を足していないか
- 差分は読みやすく、戻しやすいか
- git 状態は把握できているか
- 破壊的な git 操作を独断でしていないか
- ビルド・動作・互換性の確認が必要な範囲を見落としていないか
- **軽量化の名目で総合的な処理量を増やしていないか**
- **余計な機能や保険処理を足していないか**
- **コード全体の整合性が保たれているか**